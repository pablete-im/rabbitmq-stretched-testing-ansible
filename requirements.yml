---
# Ansible collection dependencies
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  # - name: community.vmware # Not needed for manual provisioning
  #   version: ">=4.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
